version: '3.8'

services:
  cvwebsite:
    image: nginx:alpine
    container_name: cvwebsite
    volumes:
      - /home/jean/cvWebsite/html:/usr/share/nginx/html:ro
      - /home/jean/cvWebsite/html/css:/usr/share/nginx/html/css:ro
      - /home/jean/cvWebsite/html/js:/usr/share/nginx/html/js:ro
    networks:
      - web

  airwebsite:
    image: nginx:alpine
    container_name: airwebsite
    volumes:
      - /home/jean/airWebsite/html:/usr/share/nginx/html:ro
      - /home/jean/airWebsite/html/css:/usr/share/nginx/html/css:ro
      - /home/jean/airWebsite/html/js:/usr/share/nginx/html/js:ro
    networks:
      - web

  nginx:
    image: nginx:alpine
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - /home/jean/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - cvwebsite
      - airwebsite
    networks:
      - web

networks:
  web:
    driver: bridge
